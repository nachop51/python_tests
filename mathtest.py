import math

# en realidad esto es lo mismo que javascript, voy a dejar una lista de funciones
# math.pow(x, y) x elevado a y
# math.sqrt(x, y)
# math.ceil(x) <- redondea para arriba siempre
# math.floor(x) <- redondea para abajo siempre
# round(x) <- redondea por aproximacion
# math.pi <- devuelve pi
# math.e <- devuelve e
# n = [1, 2, 3]
# sum(n) <- suma una lista, devolveria 6
# math.fsum(n) <- tambien suma una lista, pero lo hace con mucha mas precision
# math.trunc(x) <- hace truncamiento de numeros enteros
# lo que hace es sacar toda la parte decimal y devolver la parte entera
# tambien se puede usar el math.pow de la fomra normal de python
# x ** y <- es lo mismo que x elevado a y